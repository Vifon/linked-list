cmake_minimum_required(VERSION 2.8)
project(list)
add_definitions(-O2 -pedantic -Wall -ansi)
if (cfg STREQUAL "debug")
  add_definitions(-g -O0)
else()
  set(CMAKE_C_COMPILER clang)
  set(CMAKE_CXX_COMPILER clang++)
endif()
add_subdirectory(src)
